#include <iostream>
#include <cmath>

using namespace std;
int N(int const n_fak);
void Swap(int & left,float  & right);
int main()
{
  int summa{ N(10)};
  cout<< summa << endl;
  int heltatl = 2+ 2;
  int h = 2+2.5;
  
  int a {5};
  float b =10.5;
  Swap(a,b);
  cout << a << b<< endl;

}
int N(int const n_fak)
{
  int summa{1};
  for(int i =1; i<n_fak+1;i++)
    {
      summa *= i;
    }
  return summa;
}
void Swap(int & left,float & right)
{
  int b = left;
  left= right;
  right = b;

}
